<main>
	<h1>Astro Remote</h1>
	<section class="page-note">
		<h2>What this page does</h2>
		<p>
			This is the remote app running by itself. It exposes <code>astro_remote/widget</code>,
			<code>astro_remote/server</code>, and <code>astro_remote/RemoteCard</code> for the host to
			consume through Module Federation.
		</p>
		<p>
			The standalone widget below is only a local preview so you can confirm the remote is alive.
		</p>
		<pre class="snippet"><code><span class="token property">exposes</span><span class="token punctuation">:</span> <span class="token punctuation">{</span>
  <span class="token string">'./widget'</span><span class="token punctuation">:</span> <span class="token string">'./src/widget.ts'</span><span class="token punctuation">,</span>
  <span class="token string">'./server'</span><span class="token punctuation">:</span> <span class="token string">'./src/server.ts'</span><span class="token punctuation">,</span>
  <span class="token string">'./RemoteCard'</span><span class="token punctuation">:</span> <span class="token string">'./src/RemoteCard.astro'</span><span class="token punctuation">,</span>
<span class="token punctuation">}</span></code></pre>
	</section>
	<section class="remote-boundary boundary-standalone">
		<p class="boundary-label">Standalone widget boundary</p>
		<div id="remote-standalone"></div>
	</section>
</main>

<script>
	import '../scripts/standalone.js';
</script>

<style>
	main {
		font-family: ui-sans-serif, system-ui, sans-serif;
		max-width: 720px;
		margin: 0 auto;
		padding: 2.5rem 1rem;
	}

	h1 {
		margin-top: 0;
	}

	.page-note {
		border: 1px solid rgba(15, 23, 42, 0.16);
		border-radius: 14px;
		background: #f8fafc;
		padding: 1rem;
		margin: 0 0 1rem;
	}

	.page-note h2 {
		margin: 0 0 0.5rem;
		font-size: 1rem;
	}

	.remote-boundary {
		position: relative;
		margin-top: 1rem;
		padding: 1.25rem 1rem 1rem;
		border: 2px dashed #8b5cf6;
		border-radius: 18px;
		background: linear-gradient(180deg, rgba(245, 243, 255, 0.92), rgba(255, 255, 255, 0.96));
		box-shadow: 0 0 0 4px rgba(139, 92, 246, 0.08);
	}

	.boundary-label {
		position: absolute;
		top: -0.75rem;
		left: 0.9rem;
		margin: 0;
		padding: 0.2rem 0.6rem;
		border-radius: 999px;
		background: #4c1d95;
		color: #ede9fe;
		font-size: 0.8rem;
		font-weight: 700;
		letter-spacing: 0.02em;
	}

	.page-note .snippet {
		margin: 0.75rem 0 0;
		padding: 0.85rem 1rem;
		border-radius: 12px;
		background: #0f172a;
		color: #e2e8f0;
		overflow-x: auto;
	}

	.page-note .snippet code {
		font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
		font-size: 0.95rem;
	}

	.token.string {
		color: #86efac;
	}

	.token.property {
		color: #7dd3fc;
	}

	.token.operator,
	.token.punctuation {
		color: #94a3b8;
	}
</style>
